services:
  postgres:
    container_name: db-arena
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=db-arena

  redis:
    image: redis:latest
    ports:
      - "6379:6379"